{
  "name": "budge-it",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop budge-it",
    "restart": "pm2 restart budge-it",
    "status": "pm2 status",
    "logs": "pm2 logs budge-it",
    "build": "npm run build --prefix app",
    "deploy": "npm run build && pm2 start ecosystem.config.js || pm2 restart budge-it"
  }
}
